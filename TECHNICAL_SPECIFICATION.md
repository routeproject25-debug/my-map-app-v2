# 📋 ТЕХНІЧНЕ ЗАВДАННЯ
## Веб-додаток для управління логістичними маршрутами

---

## 1. ЗАГАЛЬНІ ВІДОМОСТІ

### 1.1 Найменування системи
**my-map-app-v2** — Система управління логістичними маршрутами з інтерактивною картою

### 1.2 Замовник
Логістична компанія (агрохолдинг)

### 1.3 Підстави для розробки
Необхідність автоматизації процесів:
- Планування маршрутів доставки сільськогосподарської продукції
- Погодження маршрутів між відділами
- Контроль витрат на логістику
- Аналітика та звітність

### 1.4 Призначення системи
Забезпечення ефективного управління логістичними маршрутами між виробничими підрозділами, складами та точками доставки з можливістю:
- Візуального планування маршрутів на карті
- Контролю доступу за ролями та географічними зонами (хабами)
- Автоматичного розрахунку вартості доставки
- Погодження маршрутів службою безпеки
- Експорту даних для звітності

### 1.5 Дата складання
12 грудня 2025 року

---

## 2. ЦІЛІ ТА ЗАВДАННЯ

### 2.1 Основні цілі
1. **Оптимізація логістики:**
   - Скорочення часу планування маршрутів на 70%
   - Зменшення помилок у розрахунку відстаней до 0%
   - Прискорення процесу погодження до 1 дня

2. **Контроль доступу:**
   - Розмежування прав доступу між підрозділами
   - Аудит всіх дій користувачів
   - Захист конфіденційної інформації про маршрути

3. **Аналітика та звітність:**
   - Автоматична генерація звітів
   - Експорт даних у різних форматах
   - Статистика використання системи

### 2.2 Завдання системи
- Створення та редагування маршрутів на інтерактивній карті
- Зберігання довідника точок (елеватори, склади, поля)
- Автоматичний розрахунок відстаней
- Workflow погодження маршрутів
- Управління користувачами та правами доступу
- Розрахунок вартості маршрутів за розцінками
- Експорт даних у Excel, JSON, KML
- Інтеграція з Telegram для сповіщень

---

## 3. ВИМОГИ ДО СИСТЕМИ

### 3.1 Функціональні вимоги

#### 3.1.1 Автентифікація та авторизація

**FR-1.1** Система ПОВИННА підтримувати вхід через:
- Email та пароль
- Google OAuth 2.0

**FR-1.2** Система ПОВИННА підтримувати 4 основні ролі:
- **admin** — повний доступ до всіх функцій
- **logist** — створення та редагування маршрутів
- **user** — тільки перегляд маршрутів
- **security** — погодження маршрутів

**FR-1.3** Система ПОВИННА підтримувати додаткові ролі (additionalRoles):
- Користувач може мати основну роль + масив додаткових
- Приклад: logist + security

**FR-1.4** Система ПОВИННА обмежувати доступ за географічними зонами (хабами):
- Логісти бачать тільки маршрути своїх хабів
- Адміни та security бачать всі хаби
- Порожній список хабів = доступ до всіх хабів

**FR-1.5** Нові користувачі отримують роль "pending" і не можуть використовувати систему до підтвердження адміністратором

#### 3.1.2 Робота з картою

**FR-2.1** Система ПОВИННА відображати інтерактивну карту на базі Mapbox GL JS з:
- Базовими стилями: вулиці, супутник, топографія
- Підтримкою українських підписів (name_uk)
- Геолокацією користувача

**FR-2.2** Система ПОВИННА дозволяти малювати маршрути:
- Клік по карті додає точку маршруту
- Подвійний клік завершує малювання
- Можливість відміни останньої точки
- Візуальний preview лінії під час малювання

**FR-2.3** Система ПОВИННА відображати геозони (векторні шари):
- Завантаження з Mapbox Tilesets
- Заливка та обведення зон
- Попапи з інформацією про зону при кліку
- Фільтрація за хабами користувача
- Підсвічування зон "Від/До" для вибраного маршруту

**FR-2.4** Система ПОВИННА автоматично заповнювати поля форми при кліку на геозону:
- Код та назва точки
- Координати центру зони
- Підрозділ та компанія (якщо є у properties)

#### 3.1.3 Створення та редагування маршрутів

**FR-3.1** Форма маршруту ПОВИННА містити поля:
- **Хаб** (обов'язково) — географічна зона
- **Від** (обов'язково) — початкова точка з довідника
- **До** (обов'язково) — кінцева точка з довідника
- **Проміжні пункти** (опціонально) — масив точок
- **Тип маршруту** — Основний, Альтернативний1, Альтернативний2
- **Призначення** — вид продукції (пшениця, кукурудза, соняшник тощо)
- **Відстань** (автоматично) — розрахунок за координатами
- **Коментар** — додаткова інформація

**FR-3.2** Система ПОВИННА автоматично генерувати:
- Унікальний код маршруту (буква початкової точки + 10 цифр)
- Ключ маршруту (routeKey) у форматі "КодВід-КодДо"
- Відстань у кілометрах з точністю до 3 знаків після коми

**FR-3.3** Система ПОВИННА зберігати координати маршруту:
- Масив точок [lng, lat]
- Стиснуті точки (LZ-String compression) для економії місця
- Домальовані сегменти (якщо логіст скорегував автоматичну лінію)

**FR-3.4** Система ПОВИННА дозволяти редагувати існуючі маршрути:
- Завантаження маршруту зі списку
- Відображення на карті
- Зміна будь-яких полів
- Збереження з оновленням часової мітки (updatedAt)

**FR-3.5** Система ПОВИННА підтримувати м'яке видалення (soft delete):
- Поле isActive = false замість фізичного видалення
- Маршрути з isActive=false не відображаються у списках
- Адміністратор може відновити видалений маршрут

#### 3.1.4 Довідник точок (Directory)

**FR-4.1** Система ПОВИННА зберігати довідник точок маршрутів:
- Код точки (унікальний у межах хабу)
- Назва точки
- Координати (lat, lng)
- Підрозділ
- Компанія
- Хаб
- Прапорець isTest (для тестових точок)

**FR-4.2** Система ПОВИННА забезпечити швидкий пошук точок:
- Автозаповнення у формі маршруту
- Фільтрація за підрозділом
- Кешування у localStorage (TTL 12 годин)

**FR-4.3** Система ПОВИННА дозволяти імпорт довідника:
- Завантаження Excel файлу
- Валідація даних (координати, обов'язкові поля)
- Масове додавання/оновлення записів

#### 3.1.5 Погодження маршрутів

**FR-5.1** Маршрут ПОВИНЕН мати статус погодження:
- **pending** — очікує розгляду
- **approved** — погоджено
- **rejected** — відхилено
- **null** — не потребує погодження (legacy)

**FR-5.2** Користувачі з роллю security або admin ПОВИННІ мати можливість:
- Переглядати маршрути на погодження
- Погоджувати маршрут
- Відхиляти з вказанням причини
- Пропонувати зміни (proposal)

**FR-5.3** Система ПОВИННА зберігати інформацію про рішення:
- Хто прийняв рішення (decisionBy)
- Коли прийнято (decisionAt)
- Коментар (для відхилених)

**FR-5.4** Система ПОВИННА відправляти Telegram повідомлення:
- При створенні нового маршруту на погодження
- При відправці на повторне погодження
- З посиланням на сторінку погодження

**FR-5.5** Система ПОВИННА підтримувати пропозиції змін (proposal):
- Security може запропонувати альтернативний маршрут
- Збереження оригіналу + пропозиції
- Порівняння на карті (оригінал vs пропозиція)
- Логіст може прийняти або відхилити пропозицію

#### 3.1.6 Експорт даних

**FR-6.1** Система ПОВИННА дозволяти експорт у форматах:
- **Excel (.xlsx)** — таблиця з видимими колонками
- **JSON** — повна структура маршруту
- **KML** — для Google Earth/Maps
- **ZIP** — архіви для множинного експорту

**FR-6.2** Експорт Excel ПОВИНЕН включати колонки:
- Хаб, Від (код/назва), До (код/назва)
- Підрозділ, Проміжні пункти, Відстань
- Тип, Призначення, Логіст
- Статус погодження, Дати створення/зміни/рішення
- Коментар/причина відхилення

**FR-6.3** Система ПОВИННА дозволяти фільтрацію перед експортом:
- За хабом, періодом, типом, статусом
- За точками "Від/До"
- За підрозділом
- За логістом
- За відстанню (діапазон)
- За тестовими маршрутами (для admin)

**FR-6.4** Таблиця експорту ПОВИННА мати функції:
- Сортування за будь-якою колонкою
- Excel-like фільтри (dropdown з чекбоксами)
- Пагінація (1000 записів за раз)
- Керування видимістю колонок
- Автоширина / ручне розтягування колонок
- Два липкі скролбари (верхній та нижній)

**FR-6.5** Система ПОВИННА показувати карту внизу сторінки експорту:
- Клік по рядку → відображення маршруту на карті
- Притягування до доріг (Mapbox Directions API)
- Підсвічування геозон
- Змінюваний розмір панелі карти

#### 3.1.7 Імпорт даних

**FR-7.1** Система ПОВИННА дозволяти імпорт довідника точок:
- Завантаження Excel файлу
- Автоматичне визначення колонок
- Валідація координат (формати: 50.45, 50°27'18"N)
- Попередній перегляд перед збереженням

**FR-7.2** Система ПОВИННА дозволяти імпорт готових маршрутів:
- Завантаження JSON або Excel
- Валідація структури даних
- Автоматична генерація routeKey
- Масове додавання у Firestore

#### 3.1.8 Калькулятор вартості

**FR-8.1** Система ПОВИННА розраховувати вартість маршруту:
- Вибір хабу, року, культури
- Введення відстані
- Пошук у довіднику розцінок (pricing)
- Відображення ціни з ПДВ / без ПДВ

**FR-8.2** Довідник розцінок ПОВИНЕН містити:
- Діапазони відстаней (від-до км)
- Тип ціни (т/км або за тону)
- Ціни з ПДВ та без ПДВ
- Прив'язка до хабу, року, культури

#### 3.1.9 Управління користувачами (для admin)

**FR-9.1** Адміністратор ПОВИНЕН мати можливість:
- Переглядати список всіх користувачів
- Створювати нових користувачів (Email/Password)
- Редагувати ролі та хаби
- Призначати додаткові ролі (масив)
- Видаляти користувачів (з Auth + Firestore)

**FR-9.2** Система ПОВИННА відображати для кожного користувача:
- Email, ім'я, роль, додаткові ролі
- Список дозволених хабів
- Прапорець testMode
- Дату реєстрації

**FR-9.3** Система ПОВИННА підтримувати тестовий режим (testMode):
- Користувач з testMode=true бачить тільки тестові маршрути
- Тестові маршрути: isTest=true або між тестовими точками
- Адміністратор може увімкнути/вимкнути testMode

#### 3.1.10 Статистика відвідувань (для admin)

**FR-10.1** Система ПОВИННА відстежувати відвідування:
- Автоматичний старт сесії при завантаженні
- Heartbeat кожні 30 секунд
- Завершення сесії при закритті вкладки
- Унікальний ID відвідувача (localStorage)

**FR-10.2** Система ПОВИННА відображати метрики:
- 🟢 Онлайн зараз (активні останні 2 хвилини)
- 📅 Відвідувань сьогодні
- 👥 Унікальних відвідувачів сьогодні
- 📊 Відвідувань за тиждень
- 🌐 Всього відвідувань

**FR-10.3** Статистика ПОВИННА зберігатися:
- visitor_sessions — кожна сесія окремо
- visitor_stats — агрегована статистика по днях

#### 3.1.11 Адміністрування

**FR-11.1** Система ПОВИННА надавати інструменти адміністрування:
- **Firestore Viewer** — перегляд/редагування колекцій
- **GeoJSON Builder** — створення геозон вручну
- **Tilesets Manager** — управління векторними шарами
- **Import Codes** — масовий імпорт кодів

**FR-11.2** Tilesets Manager ПОВИНЕН дозволяти:
- Додавання нових тайлсетів (mapbox:// URL)
- Налаштування layer, sourceLayer
- Призначення хабів для геозон
- Увімкнення/вимкнення шарів
- Збереження у config/tilesets.json

#### 3.1.12 Навігація

**FR-12.1** Система ПОВИННА генерувати посилання для навігаторів:
- Google Maps
- Waze
- Apple Maps
- Yandex Navigator

**FR-12.2** Посилання ПОВИННІ містити:
- Початкову точку (origin)
- Кінцеву точку (destination)
- Проміжні точки (waypoints)

**FR-12.3** Система ПОВИННА підтримувати Web Share API:
- Поділитися маршрутом через мобільні додатки
- Копіювання посилання у буфер обміну

### 3.2 Нефункціональні вимоги

#### 3.2.1 Продуктивність

**NFR-1.1** Час завантаження головної сторінки НЕ ПОВИНЕН перевищувати 3 секунди

**NFR-1.2** Відгук на дії користувача (клік, введення) ПОВИНЕН бути до 100 мс

**NFR-1.3** Таблиця експорту ПОВИННА відображати до 10000 рядків без зависання браузера

**NFR-1.4** Карта ПОВИННА працювати плавно (60 FPS) при зумі та переміщенні

#### 3.2.2 Масштабованість

**NFR-2.1** Система ПОВИННА підтримувати до 100 одночасних користувачів

**NFR-2.2** База даних ПОВИННА забезпечувати швидкий пошук при:
- До 100,000 маршрутів
- До 10,000 точок у довіднику
- До 1,000 користувачів

**NFR-2.3** Firestore індекси ПОВИННІ бути оптимізовані для:
- Фільтрації за хабом + датою
- Фільтрації за статусом + датою
- Пошуку за кодом/назвою

#### 3.2.3 Надійність

**NFR-3.1** Час безвідмовної роботи (uptime) ПОВИНЕН бути не менше 99.5%

**NFR-3.2** Дані користувачів ПОВИННІ бути захищені від втрати:
- Автоматичний backup Firestore (Firebase Backup)
- Soft delete для маршрутів (можливість відновлення)

**NFR-3.3** Система ПОВИННА коректно обробляти помилки:
- Помилки мережі — повторні спроби + повідомлення
- Помилки Firestore — fallback на сервер або повідомлення
- Помилки API (Mapbox, Telegram) — graceful degradation

#### 3.2.4 Безпека

**NFR-4.1** Аутентифікація ПОВИННА використовувати:
- Firebase Authentication (стандарт OAuth 2.0)
- HTTPS для всіх з'єднань
- Захист від CSRF

**NFR-4.2** Firestore Security Rules ПОВИННІ забезпечувати:
- Читання тільки своїх даних (за роллю + хабами)
- Запис тільки з валідацією полів
- Заборону прямого доступу до чужих даних

**NFR-4.3** Секретні дані ПОВИННІ зберігатися:
- API ключі — у змінних середовища
- Firebase secrets — через defineSecret
- Cloudflare secrets — через wrangler secret

**NFR-4.4** Сесії користувачів ПОВИННІ:
- Автоматично завершуватися при видаленні користувача
- Мати timeout (Firebase Auth default)
- Перевірятися на валідність при кожному запиті

#### 3.2.5 Сумісність

**NFR-5.1** Система ПОВИННА працювати у браузерах:
- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- Mobile Safari (iOS 14+)
- Chrome Mobile (Android 10+)

**NFR-5.2** Система ПОВИННА бути адаптивною:
- Desktop (> 980px) — повна функціональність
- Tablet (768-980px) — адаптований UI
- Mobile (< 768px) — спрощений UI + touch-friendly

**NFR-5.3** Система НЕ ПОВИННА вимагати установки:
- Веб-додаток (SPA)
- Без плагінів (Flash, Java)
- PWA-ready (можливість додати на головний екран)

#### 3.2.6 Зручність використання (Usability)

**NFR-6.1** Інтерфейс ПОВИНЕН бути інтуїтивним:
- Очевидні назви кнопок та меню
- Підказки (tooltips) для незрозумілих елементів
- Хлібні крихти (breadcrumbs) для навігації

**NFR-6.2** Помилки ПОВИННІ відображатися зрозуміло:
- Українською мовою
- З описом дії для виправлення
- З можливістю закриття

**NFR-6.3** Система ПОВИННА давати зворотній зв'язок:
- Індикатори завантаження (spinner)
- Повідомлення про успішне збереження
- Підтвердження перед видаленням

**NFR-6.4** Дизайн ПОВИНЕН бути єдиним:
- Спільна кольорова схема (фіолетовий #6366f1)
- Єдині компоненти (кнопки, форми, картки)
- Спільний header на всіх сторінках

#### 3.2.7 Локалізація

**NFR-7.1** Мова інтерфейсу — українська

**NFR-7.2** Підписи на карті ПОВИННІ відображатися:
- name_uk (пріоритет)
- name_ru (якщо немає UK)
- name_en (якщо немає RU)
- name (fallback)

**NFR-7.3** Формати даних:
- Дати: `DD.MM.YYYY HH:MM:SS`
- Числа: кома як десятковий роздільник
- Відстані: кілометри (км)

#### 3.2.8 Документація

**NFR-8.1** Проект ПОВИНЕН містити документацію:
- README.md — швидкий старт
- PROJECT_DOCUMENTATION.md — повна документація
- TECHNICAL_SPECIFICATION.md — технічне завдання
- Inline коментарі у коді для складних секцій

**NFR-8.2** README файли для окремих модулів:
- export/README.md
- styles/README.md
- DEPLOY_STATS.md
- VISITOR_STATS_README.md

---

## 4. ТЕХНІЧНА АРХІТЕКТУРА

### 4.1 Frontend

**Технології:**
- HTML5, CSS3, JavaScript (ES6+)
- Mapbox GL JS 2.15.0 — інтерактивна карта
- Firebase JS SDK 10.12.2 (compat) — автентифікація, Firestore
- SheetJS (xlsx) 0.18.5 — експорт/імпорт Excel
- LZ-String 1.5.0 — стискання даних
- JSZip 3.10.1 — створення архівів

**Структура:**
- Single Page Application (SPA)
- Модульна архітектура (окремі HTML файли для розділів)
- Спільні компоненти (common.css, header.js)
- localStorage для кешування (довідник, хаби, роль)

### 4.2 Backend

**Платформа:** Firebase (Google Cloud)

**Компоненти:**
1. **Firebase Authentication** — управління користувачами
2. **Cloud Firestore** — NoSQL база даних
3. **Cloud Functions (Node.js)** — серверна логіка
4. **Firebase Hosting** — хостинг статичних файлів
5. **Cloud Storage** — (резерв для файлів)

**Додатково:**
- **Cloudflare Workers** — альтернатива для Telegram API
- **Express Server (server.js)** — локальний dev сервер

### 4.3 База даних (Firestore)

**Колекції:**

| Колекція | Призначення | Приклад документа |
|----------|-------------|-------------------|
| `users` | Користувачі | {uid, email, role, additionalRoles[], allowedHubs[], testMode} |
| `routes` | Маршрути | {code, routeKey, hub, fromCode, toCode, points[], approval, isActive, isTest} |
| `directory` | Довідник точок | {[hub]: {items: [{code, name, lat, lng, unit, company}]}} |
| `pricing` | Розцінки | {hub, year, crop, ranges: [{distFrom, distTo, price}]} |
| `visitor_sessions` | Сесії відвідувачів | {userId, startTime, lastHeartbeat, endTime, page} |
| `visitor_stats` | Статистика по днях | {date, totalVisits, uniqueVisitors[]} |
| `roles` | (legacy) Дзеркало ролей | {uid, role} |
| `logists` | Логісти | {uid, name, email} |

**Індекси (firestore.indexes.json):**
- routes: hub + updatedAt
- routes: hub + routeType + updatedAt
- visitor_sessions: date + lastHeartbeat

**Правила безпеки (firestore.rules):**
- Читання routes: за роллю + хабами
- Запис routes: admin, logist (тільки свої хаби)
- Погодження: admin, security
- users: читання свого профілю, запис тільки admin

### 4.4 Cloud Functions

**Функції:**

1. **telegramSendApproval** (HTTP)
   - Відправка Telegram повідомлення
   - POST `/api/telegram/send-approval`
   - Секрети: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID

2. **saveRoute** (HTTP)
   - Серверне збереження маршруту (fallback)
   - POST `/api/routes/save`
   - Валідація + генерація routeKey

3. **approveRoute** (HTTP)
   - Погодження/відхилення маршруту
   - POST `/api/routes/approve`
   - Оновлення approval статусу

4. **deleteUser** (HTTP)
   - Видалення користувача
   - POST `/api/users/delete`
   - Видалення з Auth + Firestore

**Тригери (onDocumentCreated, onDocumentUpdated):**
- (Резерв для майбутніх автоматизацій)

### 4.5 Cloudflare Worker

**Призначення:** Швидка альтернатива Firebase Function для Telegram

**Endpoint:** `https://route-telegram-endpoint.route-project25.workers.dev/send-approval`

**Переваги:**
- Швидше (edge computing)
- Дешевше (100k req/day безкоштовно)
- Підтримка CORS

### 4.6 Інтеграції

**Зовнішні API:**

1. **Mapbox API:**
   - GL JS — карта
   - Tilesets API — геозони
   - Directions API — притягування до доріг
   - Geocoding API — (резерв)

2. **Telegram Bot API:**
   - sendMessage — нотифікації
   - parse_mode: HTML

3. **Firebase REST API:**
   - Authentication
   - Firestore
   - Cloud Functions

**Ліміти:**
- Mapbox: 100,000 map loads/month (free tier)
- Mapbox Directions: 100,000 req/month
- Firebase: 50k reads, 20k writes/day (free tier)
- Telegram: необмежені повідомлення

### 4.7 Деплой та хостинг

**Production:**
- Firebase Hosting: `route-project-ba7c6.web.app`
- Cloudflare Worker: `route-telegram-endpoint.route-project25.workers.dev`

**Development:**
- Local server: `http://localhost:3000` (Express)
- Firebase Emulators: Auth, Firestore, Functions

**CI/CD:**
- Git: GitHub (routeproject25-debug/my-map-app-v2)
- Деплой: `firebase deploy` (manual)

---

## 5. ІНТЕРФЕЙС КОРИСТУВАЧА

### 5.1 Дизайн-система

**Кольорова схема:**
- Основний: `#6366f1` (фіолетовий)
- Успіх: `#22c55e` (зелений)
- Помилка: `#ef4444` (червоний)
- Попередження: `#f59e0b` (помаранчевий)
- Нейтральний: `#64748b` (сірий)

**Типографіка:**
- Шрифт: system-ui, -apple-system, 'Segoe UI', sans-serif
- Розміри: 12px, 14px, 16px, 18px, 20px, 24px
- Вага: 400 (normal), 500 (medium), 600 (semibold), 700 (bold)

**Компоненти:**
- Кнопки: primary, secondary, ghost, danger
- Форми: input, select, textarea
- Картки: card, shadow
- Бейджі: badge
- Повідомлення: alert (success, error, warning, info)

**Відступи:**
- Spacing: 4px, 8px, 12px, 16px, 24px, 32px, 48px
- Радіуси: 4px, 8px, 12px, 16px
- Тіні: 3 рівні (sm, md, lg)

### 5.2 Сторінки системи

#### 5.2.1 Вхід (login.html)
- Форма Email/Password
- Кнопка Google Sign-In
- Повідомлення про очікування підтвердження (pending)
- Фонова анімація з градієнтом

#### 5.2.2 Головна (index.html)
- Header з навігацією та профілем
- Карта (повна висота вікна)
- Панель управління (ліворуч):
  - Кнопки малювання
  - Форма маршруту
  - Список збережених маршрутів
- Док стилів карти (праворуч знизу)
- Статистика відвідувань (dropdown у header, тільки admin)

#### 5.2.3 Експорт (export/index.html)
- Header
- Панель фільтрів (згорнута/розгорнута)
- Чекбокси видимості колонок (ліворуч, згорнуті/розгорнуті)
- Таблиця з горизонтальними скролбарами
- Кнопки експорту (Excel, JSON, KML)
- Карта внизу (опціонально)

#### 5.2.4 Погодження (review.html)
- Список маршрутів на погодження
- Фільтр за статусом
- Карта з маршрутом
- Кнопки: Погодити / Відхилити / Запропонувати зміни

#### 5.2.5 Адмін (admin.html)
- Таблиця користувачів
- Форма редагування користувача
- Кнопки: Створити / Редагувати / Видалити
- Чекбокс testMode

#### 5.2.6 Калькулятор (calculator.html)
- Форма вибору параметрів
- Таблиця розцінок
- Результат розрахунку

#### 5.2.7 Навігація (navigate.html)
- Інформація про маршрут
- Кнопки навігаторів
- Web Share

#### 5.2.8 Імпорт (import/index.html, import/upload-routes.html)
- Завантаження файлу
- Попередній перегляд
- Валідація
- Кнопка "Імпортувати"

#### 5.2.9 Адмін інструменти
- admin/tilesets.html — управління геозонами
- admin/geojson-builder.html — створення GeoJSON
- admin/import-codes.html — імпорт кодів
- Firestore_Viewe.html — перегляд Firestore

### 5.3 Адаптивність

**Desktop (> 980px):**
- Повна функціональність
- Панелі поруч з картою
- Таблиці на всю ширину

**Tablet (768-980px):**
- Згортання бокових панелей
- Адаптація таблиць
- Touch-friendly кнопки

**Mobile (< 768px):**
- Burger меню
- Панелі під картою
- Горизонтальний скрол таблиць
- Збільшені кнопки

---

## 6. ТЕСТУВАННЯ

### 6.1 Типи тестування

**Функціональне:**
- Створення маршруту
- Редагування маршруту
- Видалення маршруту
- Погодження/відхилення
- Експорт у всі формати
- Імпорт довідника

**Безпеки:**
- Спроба доступу без входу
- Спроба доступу з роллю pending
- Спроба читання чужих хабів
- Спроба запису з роллю user

**Продуктивності:**
- Завантаження 10000 рядків у таблицю
- Малювання складного маршруту (100+ точок)
- Одночасна робота 50 користувачів

**Сумісності:**
- Тестування у всіх браузерах
- Тестування на мобільних пристроях
- Різні розширення екрану

### 6.2 Тестові сценарії

**TS-1: Створення маршруту логістом**
1. Увійти як logist
2. Клікнути "Намалювати маршрут"
3. Клікнути на карті 5 разів (точки)
4. Подвійний клік для завершення
5. Заповнити форму (Від, До, Тип, Призначення)
6. Натиснути "Зберегти"
7. Перевірити, що маршрут з'явився у списку

**Очікуваний результат:** Маршрут збережено, статус "pending"

**TS-2: Погодження маршруту**
1. Увійти як security
2. Перейти на /review.html
3. Вибрати маршрут зі статусом pending
4. Переглянути на карті
5. Натиснути "Погодити"
6. Перевірити Telegram чат (має прийти повідомлення)

**Очікуваний результат:** Статус змінено на "approved", повідомлення у Telegram

**TS-3: Експорт у Excel**
1. Увійти як logist
2. Перейти на /export/
3. Встановити фільтр: Хаб = "Hub1", Період = "Останній місяць"
4. Натиснути "Попередній перегляд"
5. Перевірити таблицю (тільки Hub1)
6. Натиснути "Експорт у Excel"
7. Відкрити завантажений файл

**Очікуваний результат:** Excel файл з відфільтрованими маршрутами

**TS-4: Спроба доступу без прав**
1. Увійти як user
2. Спробувати перейти на /admin.html
3. Спробувати створити маршрут (клік "Намалювати")

**Очікуваний результат:** Редірект на /login.html або /index.html, повідомлення "Доступ заборонено"

### 6.3 Критерії прийняття

**Функціональні:**
- ✅ Всі основні сценарії працюють без помилок
- ✅ Firestore rules блокують неавторизовані дії
- ✅ Експорт генерує валідні файли
- ✅ Telegram нотифікації доставляються

**Продуктивність:**
- ✅ Час завантаження < 3 сек
- ✅ Відгук на клік < 100 мс
- ✅ Таблиця 10k рядків відкривається < 5 сек

**Безпека:**
- ✅ Немає витоку даних між хабами
- ✅ Роль pending не має доступу до функцій
- ✅ API ключі не доступні через браузер

**Зручність:**
- ✅ Користувач розуміє інтерфейс без інструкції
- ✅ Помилки відображаються зрозуміло
- ✅ Дизайн однорідний на всіх сторінках

---

## 7. ВПРОВАДЖЕННЯ

### 7.1 Етапи впровадження

**Етап 1: Підготовка інфраструктури (2 дні)**
- Створення Firebase проекту
- Налаштування Authentication (Email, Google)
- Створення Firestore колекцій
- Деплой Security Rules та Індексів
- Налаштування Cloud Functions
- Деплой Cloudflare Worker

**Етап 2: Імпорт початкових даних (1 день)**
- Завантаження довідника точок
- Завантаження розцінок
- Завантаження переліку хабів
- Створення адміністратора

**Етап 3: Створення користувачів (1 день)**
- Реєстрація логістів
- Реєстрація security
- Призначення ролей та хабів
- Інструктаж користувачів

**Етап 4: Міграція даних (опціонально, 2-3 дні)**
- Експорт маршрутів зі старої системи
- Конвертація у формат системи
- Імпорт через admin/upload-routes.html
- Перевірка цілісності даних

**Етап 5: Пілотний запуск (1 тиждень)**
- Робота 2-3 логістів з системою
- Моніторинг помилок
- Збір зворотнього зв'язку
- Виправлення критичних багів

**Етап 6: Повний запуск (2 тижні)**
- Підключення всіх користувачів
- Щоденний моніторинг
- Технічна підтримка
- Оптимізація продуктивності

### 7.2 Навчання користувачів

**Адміністратори (2 години):**
- Управління користувачами
- Призначення ролей та хабів
- Firestore Viewer
- Резервне копіювання
- Моніторинг статистики

**Логісти (1.5 години):**
- Малювання маршрутів на карті
- Заповнення форми маршруту
- Редагування та видалення
- Робота з довідником
- Експорт у Excel

**Security (1 година):**
- Перегляд маршрутів на погодження
- Погодження/відхилення
- Пропозиції змін
- Telegram нотифікації

**Users (0.5 години):**
- Вхід у систему
- Перегляд карти
- Пошук маршрутів
- Навігація

### 7.3 Технічна підтримка

**Рівень 1 (користувачі):**
- Email: support@company.com
- Telegram: @route_support_bot
- Час відгуку: 4 години

**Рівень 2 (розробники):**
- Firebase Console
- Cloudflare Dashboard
- GitHub Issues
- Час відгуку: 24 години (робочі дні)

**Ескалація критичних помилок:**
- Негайно (Telegram admin)
- Деплой hotfix < 2 години

---

## 8. ВИМОГИ ДО ДОКУМЕНТАЦІЇ

### 8.1 Технічна документація

**Обов'язкові документи:**
- ✅ PROJECT_DOCUMENTATION.md — повна документація проекту
- ✅ TECHNICAL_SPECIFICATION.md — технічне завдання (цей документ)
- ✅ README.md — швидкий старт для розробників
- ✅ firestore.rules — з коментарями
- ✅ config/tilesets.schema.json — схема конфігурації

**Документація модулів:**
- ✅ export/README.md
- ✅ styles/README.md
- ✅ DEPLOY_STATS.md
- ✅ VISITOR_STATS_README.md

### 8.2 Користувацька документація

**Інструкції:**
- [ ] Інструкція для логістів (PDF)
- [ ] Інструкція для security (PDF)
- [ ] Інструкція для адміністраторів (PDF)

**Відео:**
- [ ] Відео "Як створити маршрут" (3 хв)
- [ ] Відео "Як погодити маршрут" (2 хв)
- [ ] Відео "Як експортувати у Excel" (2 хв)

### 8.3 Коментарі у коді

**Обов'язково коментувати:**
- Складні алгоритми (розрахунок відстаней, фільтрація)
- Firestore запити з where()
- Критичні секції безпеки
- Workarounds для браузерів

**Формат коментарів:**
```javascript
/**
 * Розрахунок відстані між точками за формулою Haversine
 * @param {Array} points - Масив координат [[lng, lat], ...]
 * @returns {Number} Відстань у кілометрах
 */
function calculateDistance(points) { ... }
```

---

## 9. КРИТЕРІЇ ПРИЙМАННЯ РОБОТИ

### 9.1 Функціональні критерії

✅ **Автентифікація:**
- Вхід через Email/Password працює
- Вхід через Google працює
- Роль pending блокує доступ
- Автоматичний вихід при видаленні користувача

✅ **Робота з маршрутами:**
- Малювання маршруту на карті
- Збереження у Firestore
- Редагування існуючого
- Відображення геозон
- М'яке видалення (soft delete)

✅ **Погодження:**
- Security бачить тільки pending маршрути
- Погодження змінює статус на approved
- Відхилення дозволяє вказати причину
- Telegram нотифікація відправляється

✅ **Експорт:**
- Excel експорт працює
- JSON експорт працює
- KML експорт працює
- Фільтри застосовуються правильно
- Карта внизу відображає маршрут

✅ **Управління:**
- Адмін може створювати користувачів
- Адмін може призначати ролі
- Адмін може призначати хаби
- Адмін бачить статистику відвідувань

### 9.2 Технічні критерії

✅ **Продуктивність:**
- Головна сторінка завантажується < 3 сек
- Таблиця 1000 рядків відображається < 1 сек
- Карта працює плавно (60 FPS)

✅ **Безпека:**
- Firestore rules тестовані (emulator)
- Немає витоку даних між хабами
- API ключі в env змінних

✅ **Сумісність:**
- Працює у Chrome 90+
- Працює у Firefox 88+
- Працює у Safari 14+
- Адаптивний дизайн на мобільних

✅ **Код:**
- Немає критичних помилок у консолі
- Код відформатований та структурований
- Коментарі для складних секцій
- Git історія читабельна

### 9.3 Документація

✅ **Наявність:**
- PROJECT_DOCUMENTATION.md
- TECHNICAL_SPECIFICATION.md
- README.md
- Inline коментарі

✅ **Актуальність:**
- Документація відповідає коду
- Приклади працюють
- Посилання валідні

---

## 10. РИЗИКИ ТА ОБМЕЖЕННЯ

### 10.1 Технічні ризики

**Ризик 1: Вичерпання безкоштовного ліміту Firebase**
- Ймовірність: середня
- Вплив: високий
- Мітигація: Моніторинг використання, оптимізація запитів, кешування

**Ризик 2: Помилки Mapbox API (недоступність, зміна API)**
- Ймовірність: низька
- Вплив: середній
- Мітигація: Fallback на оригінальні точки (без snapping), кешування tiles

**Ризик 3: Повільна робота з великими таблицями (>10k рядків)**
- Ймовірність: середня
- Вплив: середній
- Мітигація: Віртуальна прокрутка (virtual scroll), серверна пагінація

**Ризик 4: Блокування Telegram у деяких мережах**
- Ймовірність: середня
- Вплив: низький
- Мітигація: Email нотифікації як альтернатива

### 10.2 Функціональні обмеження

**Обмеження 1:** Максимум 10 хабів у where() запиті Firestore
- Рішення: Якщо більше 10 — завантажувати окремими запитами або всі маршрути з фільтрацією на клієнті

**Обмеження 2:** Максимум 25 waypoints у Mapbox Directions API
- Рішення: Відображати без snapping або розбивати на сегменти

**Обмеження 3:** Експорт великих файлів (>50MB) може зависнути браузер
- Рішення: Попередження користувачу, експорт частинами

**Обмеження 4:** Offline режим не підтримується
- Рішення: PWA з service workers (майбутня версія)

### 10.3 Бізнес-ризики

**Ризик 1: Користувачі не приймуть нову систему**
- Ймовірність: низька
- Вплив: високий
- Мітигація: Навчання, збір фідбеку, поступове впровадження

**Ризик 2: Недостатньо часу на навчання**
- Ймовірність: середня
- Вплив: середній
- Мітигація: Відео інструкції, контекстні підказки, технічна підтримка

---

## 11. ПІДТРИМКА ТА РОЗВИТОК

### 11.1 План підтримки

**Регулярні дії:**
- Щоденний моніторинг помилок (Firebase Console)
- Щотижневе резервне копіювання Firestore
- Щомісячний аналіз статистики використання
- Щоквартальне оновлення залежностей

**SLA:**
- Критичні баги (система не працює) — fix за 4 години
- Високий пріоритет (функція не працює) — fix за 24 години
- Середній пріоритет (незручність) — fix за 1 тиждень
- Низький пріоритет (покращення) — у наступному релізі

### 11.2 Roadmap розвитку

**v2.1 (Q1 2026):**
- [ ] Microsoft OAuth
- [ ] Пакетне редагування маршрутів
- [ ] Історія змін (changelog)

**v2.2 (Q2 2026):**
- [ ] PWA з offline режимом
- [ ] Мобільний додаток (React Native)
- [ ] Графіки статистики (Charts.js)

**v2.3 (Q3 2026):**
- [ ] Експорт у PDF
- [ ] REST API для інтеграцій
- [ ] Webhooks

**v3.0 (Q4 2026):**
- [ ] Планувальник маршрутів (AI-асистент)
- [ ] Інтеграція з GPS трекерами
- [ ] Реальний час відстеження

---

## 12. ГЛОСАРІЙ

**Хаб (Hub)** — географічна зона, виробничий підрозділ або регіон, що об'єднує кілька точок маршруту

**Геозона** — векторний полігон на карті, що представляє територію об'єкта (елеватор, склад, поле)

**Тайлсет (Tileset)** — набір векторних даних у форматі Mapbox Vector Tiles для відображення геозон

**Маршрут** — логістичний шлях від точки А до точки Б з можливими проміжними пунктами

**routeKey** — унікальний ключ маршруту у форматі "КодВід-КодДо" (наприклад, "D001-D050")

**Довідник (Directory)** — база даних точок маршрутів з координатами та атрибутами

**Погодження (Approval)** — процес підтвердження маршруту службою безпеки або адміністратором

**Proposal** — пропозиція змін до маршруту від security

**Soft delete** — м'яке видалення (позначка isActive=false замість фізичного видалення)

**testMode** — режим роботи, коли користувач бачить тільки тестові дані

**Heartbeat** — періодичний сигнал про активність користувача (кожні 30 сек)

**Firestore** — NoSQL база даних від Google Firebase

**Cloud Function** — серверна функція без підтримки сервера (serverless)

**localStorage** — сховище даних у браузері користувача

---

## 13. ДОДАТКИ

### Додаток А: Структура JSON маршруту

```json
{
  "code": "D1732467894309",
  "routeKey": "D001-D050",
  "hub": "Hub1",
  "fromCode": "D001",
  "fromName": "Елеватор №1",
  "fromLabel": "Елеватор №1 • Підрозділ А • Компанія XYZ",
  "toCode": "D050",
  "toName": "Склад центральний",
  "toLabel": "Склад центральний • Підрозділ Б • Компанія XYZ",
  "viaPoints": [
    {
      "code": "D025",
      "name": "Проміжний склад",
      "label": "Проміжний склад • Підрозділ А • Компанія XYZ"
    }
  ],
  "routeType": "Основний",
  "purpose": "Пшениця",
  "distance_km": 45.367,
  "points": [[30.523, 50.450], [30.530, 50.455], ...],
  "srcz": "compressed-base64-string",
  "segments": [
    {
      "type": "Feature",
      "geometry": {"type": "LineString", "coordinates": [...]},
      "properties": {"segmentType": "drawn"}
    }
  ],
  "zones": {
    "from": {
      "type": "Feature",
      "geometry": {"type": "Polygon", "coordinates": [...]},
      "properties": {"Name": "Елеватор №1", "Code": "D001"}
    },
    "to": { ... }
  },
  "logisticId": "firebase-uid",
  "logisticName": "Іван Іванович",
  "approval": {
    "status": "pending",
    "decisionBy": null,
    "decisionAt": null,
    "comment": ""
  },
  "proposal": null,
  "isActive": true,
  "isTest": false,
  "createdAt": "2025-11-24T10:30:00.000Z",
  "updatedAt": "2025-11-24T10:30:00.000Z"
}
```

### Додаток Б: Приклад Firestore Security Rule

```javascript
match /routes/{routeId} {
  allow read: if authed() && canReadRoute(resource.data);
  allow create: if authed() && isWriter() && validRoute(request.resource.data);
  allow update: if authed() && (
    isWriter() && canReadRoute(resource.data) && validRoute(request.resource.data)
    || (isSecurity() || isAdmin()) && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['approval'])
  );
  allow delete: if authed() && isAdmin();
}

function canReadRoute(d) {
  return (isAdmin() || isSecurity())
      || hasAllHubs()
      || (d.hub is string && d.hub in _userAllowedHubs());
}
```

### Додаток В: Приклад деплою

```bash
# 1. Встановлення залежностей
npm install

# 2. Конфігурація Firebase
firebase login
firebase use route-project-ba7c6

# 3. Деплой Firestore
firebase deploy --only firestore:rules
firebase deploy --only firestore:indexes

# 4. Деплой Functions
cd functions
npm install
cd ..
firebase deploy --only functions

# 5. Деплой Hosting
firebase deploy --only hosting

# 6. Деплой Cloudflare Worker
cd cloudflare-worker
wrangler login
wrangler deploy
```

---

## ПІДПИСИ

**Розробник:**  
AI Assistant  
Дата: 12 грудня 2025

**Замовник:**  
_________________________  
Дата: __________________

**Затверджено:**  
_________________________  
Дата: __________________

---

**Кінець документа**
