# Mapbox Management API (server-side only)
# Create a Secret Access Token (sk...) with scope: tilesets:read
# DO NOT commit real secrets. Copy this file to .env and fill values locally.
MAPBOX_ACCESS_TOKEN=sk.your_secret_token_here
MAPBOX_OWNER=route-project

# Optional Basic Auth for server
# USER1=admin
# PASS1=adminpassword

# Telegram (server-side notifications)
# Configure these to enable approval messages from the app.
# NEVER expose them in client code or commit real values.
# TELEGRAM_BOT_TOKEN=8491491111:example-bot-token
# TELEGRAM_CHAT_ID=-123456789